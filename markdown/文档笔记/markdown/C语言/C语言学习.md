## C语言学习

c语言重点：

- 1.固定的格式：

```c
#include<stdio.h>
int main(){
    //需要编程的代码
    
    return 0;
}
```

``` c 
//printf语句：用来输出内容
printf("要输出的内容")；
    printf("要输出的内容\n"）；斜杠\n表示换行
           printf("要输出的内容\t");斜杠\t表示空几个格！
           
           例子：
           #include<stdio.h>
           int main（）{
               printf("hello world!");
                printf("hello world!\n");
                printf("hello world!\n");
                printf("hello world! \n");//此处表示换行输出
                printf("hello world!");
                printf("hello world!");
                printf("hello world! \t");//此处表示输出语句之间添加间隔；
                printf("hello world!");
               
               return 0;
           }
           int 
           float
           double
           char
  ###############################
  int a =3;
           float a = 3.14;
           double a = 3.14;
 char a = 'A';
           
           int a = 3, b = 5, c = 6
```



